package com.market.pipeline.processor;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.ActiveProfiles;

/**
 * Basic application context loading test for {@link ProcessorApplication}.
 */
@SpringBootTest
@ActiveProfiles("test")
class ProcessorApplicationTests {

    /**
     * Verifies that the Spring Boot application context loads successfully.
     */
    @Test
    void contextLoads() {
    }
}
